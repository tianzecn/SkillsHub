interface:
  display_name: "Release Publish"
  short_description: "PromptHub 提交、打 tag 与正式发布流程"
  default_prompt: "Use $release-publish to commit all PromptHub changes, tag a new version, and publish a verified GitHub Release."
